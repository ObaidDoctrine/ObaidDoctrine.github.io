/* OBAID DOCTRINE — rating backend temporarily disabled
 * The previous hosted rating endpoint has been removed.
 * This file is intentionally kept as a no-op so existing page references
 * do not break while the replacement backend is being implemented.
 */
(() => {
  // Rating UI/backend will be restored after the replacement database API is ready.
})();
